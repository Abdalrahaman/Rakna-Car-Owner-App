<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.rakna.raknagraduationproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".azerCode.MainActivityazer"/>
        <activity android:name=".azerCode.Main2Activityazer"/>

        <activity android:name=".hassan.Garag_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".hassan.MapsActivity"/>

        <activity android:name=".abdoCode.MainActivity">

        </activity>

        <activity android:name=".abdoCode.PayActivity"/>

    </application>

</manifest>